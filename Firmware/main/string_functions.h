#ifndef STRING_FUNCTIONS_H   // Include guard to prevent double inclusion
#define STRING_FUNCTIONS_H

// Include any necessary standard or other headers
#include <stdint.h>  // Example: fixed-width integers
#include <stdbool.h> // Example: boolean types
#include <string.h>
#include <stdlib.h>

// Define constants/macros

// Declare types (structs, enums, typedefs)

// Function prototypes
int process_input(char* input);


#endif // MY_HEADER_H
